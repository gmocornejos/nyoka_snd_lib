#include <complex>

void sinft (double *y, std::complex<double> *rbuffer, const int size);

int get_array_lenght (const int size);
void complete_arr (double *arr_in, double *arr_out, const int size);
